<?php
    defined('MOODLE_INTERNAL') || die();

    $plugin->component = 'local_interest_survey';
    $plugin->version   = 20251120;       // YYYYMMDD (change to today's date when you package)
    $plugin->requires  = 2024051100;     // Moodle 5.0 minimum (adjust if needed)
    $plugin->maturity  = MATURITY_STABLE;
    $plugin->release   = '1.0 (2025-11-20)';
